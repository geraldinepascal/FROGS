Tool : clusters_stat
	INPUTS :
		biom (with or without tax)

	OUTPUTS :
		summary HTML (Hierarchical classification tree, depth grapths, ...)

	PARAMETERS :
                linkage method for HC
                distance method for HC
	
	SOFTWARES :
            PROJECT
               biomTools.py
            OTHERS
               scipy

        STEPS :
